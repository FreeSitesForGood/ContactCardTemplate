{
  "name": "contact-card-template",
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./src/styles-input.css -o ./src/styles.css --minify",
    "build:html": "eleventy",
    "build": "npm run build:css && npm run build:html"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@tailwindcss/cli": "^4.1.11",
    "comment-json": "^4.2.5",
    "tailwindcss": "^4.1.11"
  }
}